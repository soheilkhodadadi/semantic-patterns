# Methodology

This is the right place for dynamic text that may change as the pipeline changes.

Suggested subsections:

## Sentence Extraction

Describe the filing universe, annual-form filter, keyword logic, and sentence extraction pipeline.

## Sentence Classification

Describe the labeling rubric, validation assets, benchmark lane, and selected preliminary classifier.

## Constructed Measures

Define `AI_Focus`, `SpecShare`, `CredAI`, `A_S`, `log_1p_A`, and `log_1p_S`.

## Regression Design

Write the baseline panel specification here using LaTeX math syntax, for example:

$$
Y_{i,t+1} = \alpha + \beta_1 \text{AI\_Focus}_{i,t} + \beta_2 \text{SpecShare}_{i,t} + \gamma X_{i,t} + \mu_i + \lambda_t + \varepsilon_{i,t}
$$
