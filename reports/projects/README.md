# Project Reports Workspace

This folder holds tracked, project-specific reporting artifacts.

Use project subfolders for:
- analytical memos
- project-level validation notes
- project-specific execution summaries
- delivery inventories that do not belong in shared registry lanes

Keep shared control-plane reporting in `reports/registry/` or existing shared report lanes.
