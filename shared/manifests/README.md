# Shared Manifests

This lane is the future normalized home for shared manifest objects.

Examples:
- source manifests
- benchmark manifests
- split registries
- promotion manifests

Current contract:
- `docs/lab/schemas/manifest_contract_v1.md`

Near-term rule:
- do not bulk-move existing manifest-like artifacts yet
- use this lane for future normalized shared manifests after explicit promotion
