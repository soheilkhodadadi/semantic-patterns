# Shared Labels

This lane is the future normalized home for shared benchmark or labeling assets that can serve more
than one project.

Examples:
- shared benchmark labels
- shared annotation reference sets
- shared label inventories

Near-term rule:
- keep current authoritative label assets where they already live until a benchmark migration plan
  promotes a replacement
