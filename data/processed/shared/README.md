# Shared Processed Data

This lane is for derived datasets that are genuinely shared across projects.

Promotion rule:
- only place a dataset here when it is reused across more than one project or clearly designed as a lab-wide asset

Project-specific derived datasets should go under `data/processed/projects/<project>/`.
